/**
 * @Description: TODO
 * @Auther: zhanglei
 * @Date: 2019/6/12 18:59
 */
package a20栈的压入弹出序列_;


public class A20IsPopOrder {


/*
    public static boolean isPopOrder(int[] pushArr, int[] popArr) {

        Stack cache = new Stack();
        Boolean result = false;

        int nextPush = 0; // 指向下一个要push的元素的index
        int nextPop = 0;  // 指向下一个要pop的元素的index
        int pop = 0;      // 指向popOrder的首个元素的index
        int push = 0;     // 指向pushOrder的首个元素的index

        if (pushArr != null && popArr != null && pushArr.length > 0){

            while (pop < popArr.length){

//                while ()
            }
        }
    }
*/


}
